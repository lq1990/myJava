server:
  port: 8003

spring:
  application:
    name: provider-ticket


eureka:
  instance:
    prefer-ip-address: true # 注册服务时，使用服务的ip
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


